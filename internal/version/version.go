package version

var (
	// Version is the current version of codehaven
	Version = "dev"
	// CommitHash is the current git commit hash
	CommitHash = "none"
	// BuildDate is the date when the binary was built
	BuildDate = "unknown"
)
